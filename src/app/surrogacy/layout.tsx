"use client"

import { SurrogacyLayout } from "@/components/surrogacy-layout"

export default function Layout({ children }: { children: React.ReactNode }) {
  return <SurrogacyLayout>{children}</SurrogacyLayout>
}
